164c164
< index 27a812e..65908d5 100644
---
> index 27a812e..9ccb73a 100644
283c283
< @@ -344,7 +311,7 @@ static void process_frame() {
---
> @@ -344,71 +311,77 @@ static void process_frame() {
292c292,302
< @@ -356,59 +323,54 @@ static void process_frame() {
---
>      debug_stream_offer_snapshot(frame.data, frame.width, frame.height);
>      g_camera->returnFrame(frame);
>  
> -    const auto inference_ms = std::chrono::duration_cast<std::chrono::milliseconds>(
> -        std::chrono::high_resolution_clock::now() - t0).count();
> +    // Microseconds: casting straight to milliseconds truncated at measurement
> +    // time, so the published figure only ever took integer values and the
> +    // pre/postprocess split below was unresolvable.
> +    const auto inference_ms = static_cast<double>(
> +        std::chrono::duration_cast<std::chrono::microseconds>(
> +            std::chrono::high_resolution_clock::now() - t0).count()) / 1000.0;
314a325,331
> +    // detectAll() as a whole: model_->run() plus this app's own filtering and
> +    // scaling. The MQTT contract asks for the wider region to be published as
> +    // pipeline_ms when the accelerator call cannot be isolated, which is the
> +    // case here (see PoseDetector::modelRunMs).
> +    ctx.pipeline_ms = static_cast<float>(inference_ms);
> +    ctx.model_run_ms = g_pose_detector->modelRunMs();
> +    ctx.postprocess_ms = g_pose_detector->postprocessMs();
376c393
< @@ -419,7 +381,10 @@ static void process_frame() {
---
> @@ -419,7 +392,10 @@ static void process_frame() {
388c405
< @@ -427,8 +392,8 @@ static void process_frame() {
---
> @@ -427,8 +403,8 @@ static void process_frame() {
399c416
< @@ -448,7 +413,10 @@ static int run_offline_rgb() {
---
> @@ -448,7 +424,10 @@ static int run_offline_rgb() {
411c428
< @@ -468,50 +436,70 @@ static int run_offline_rgb() {
---
> @@ -468,50 +447,70 @@ static int run_offline_rgb() {
500c517
< @@ -546,8 +534,16 @@ int main(int argc, char** argv) {
---
> @@ -546,8 +545,16 @@ int main(int argc, char** argv) {
519c536
< @@ -570,7 +566,7 @@ int main(int argc, char** argv) {
---
> @@ -570,7 +577,7 @@ int main(int argc, char** argv) {
529c546
< index f945dcd..fe6ee50 100644
---
> index f945dcd..44daf49 100644
532c549,557
< @@ -8,18 +8,6 @@
---
> @@ -1,6 +1,7 @@
>  #include "pose_detector.h"
>  
>  #include <algorithm>
> +#include <chrono>
>  #include <vector>
>  
>  #define TAG "PoseDetector"
> @@ -8,18 +9,6 @@
551c576
< @@ -84,17 +72,17 @@ void PoseDetector::setThreshold(float person_score) {
---
> @@ -84,17 +73,32 @@ void PoseDetector::setThreshold(float person_score) {
559a585,586
> +    last_model_run_ms_ = 0.0f;
> +    last_postprocess_ms_ = 0.0f;
565c592,606
<      if (model_->run(img) != MA_OK) {
---
> -    if (model_->run(img) != MA_OK) {
> +    // Microseconds, not milliseconds: the whole point of splitting these is to
> +    // see a difference that is under a millisecond on this device.
> +    using clock = std::chrono::high_resolution_clock;
> +    const auto elapsed_ms = [](clock::time_point from, clock::time_point to) {
> +        return static_cast<float>(
> +            std::chrono::duration_cast<std::chrono::microseconds>(to - from).count()) / 1000.0f;
> +    };
> +
> +    const auto t_run = clock::now();
> +    const ma_err_t run_status = model_->run(img);
> +    const auto t_run_end = clock::now();
> +    last_model_run_ms_ = elapsed_ms(t_run, t_run_end);
> +
> +    if (run_status != MA_OK) {
573c614
< @@ -102,42 +90,25 @@ const Subject* PoseDetector::detectPrimary(ma_img_t* img) {
---
> @@ -102,42 +106,26 @@ const Subject* PoseDetector::detectPrimary(ma_img_t* img) {
628a670
> +    last_postprocess_ms_ = elapsed_ms(t_run_end, clock::now());
634c676
< index 8c3e620..ee4432b 100644
---
> index 8c3e620..0867572 100644
662c704,718
< @@ -50,11 +51,7 @@ public:
---
> @@ -47,20 +48,29 @@ public:
>      bool inferenceFailed() const { return inference_failed_; }
>      int keypointCount() const { return keypoint_count_; }
>  
> +    // Timings for the last detectAll(), in milliseconds.
> +    //
> +    // sscma-micro runs letterbox, the CVI NPU call and raw-head decode inside a
> +    // single model_->run(), so the accelerator cannot be separated from
> +    // pre/postprocess without instrumenting that library. modelRunMs() is
> +    // therefore the narrowest inference measurement this API exposes;
> +    // postprocessMs() covers only what this wrapper adds on top of it
> +    // (threshold filtering, coordinate scaling and ordering).
> +    float modelRunMs() const { return last_model_run_ms_; }
> +    float postprocessMs() const { return last_postprocess_ms_; }
> +
674a731,738
>      int input_width_ = 640;
>      int input_height_ = 640;
>      int keypoint_count_ = 0;
> +    float last_model_run_ms_ = 0.0f;
> +    float last_postprocess_ms_ = 0.0f;
>      bool initialized_ = false;
>      bool inference_failed_ = false;
>  };
676c740
< index fb9af92..d08dbc5 100644
---
> index fb9af92..b8e9f36 100644
708c772
< @@ -108,75 +106,217 @@ json featureJson(const FallObservation& o, const FallDiagnostics& d) {
---
> @@ -108,75 +106,225 @@ json featureJson(const FallObservation& o, const FallDiagnostics& d) {
889a954,955
> +    // Unchanged in meaning and type so existing consumers and the frozen
> +    // baselines stay valid; the precise figures are the additive fields below.
894a961,966
> +    j["pipeline_ms"] = ctx.pipeline_ms;
> +    j["model_run_ms"] = ctx.model_run_ms;
> +    j["postprocess_ms"] = ctx.postprocess_ms;
> +    // Names the scope of model_run_ms so a reader never has to assume it is
> +    // accelerator-only: it is not, on this platform.
> +    j["inference_metric"] = "sscma_model_run_preprocess_npu_decode";
971c1043
< index 0a3953d..7276ac3 100644
---
> index 0a3953d..f9b52a2 100644
985c1057
< @@ -13,6 +15,7 @@ struct PayloadContext {
---
> @@ -13,6 +15,15 @@ struct PayloadContext {
988a1061,1068
> +    // detectAll() end to end, and its two halves. On this platform the
> +    // accelerator call cannot be isolated -- sscma-micro does letterbox, the
> +    // NPU call and raw-head decode inside one model_->run() -- so model_run_ms
> +    // is the narrowest inference figure available and pipeline_ms is the wider
> +    // region the MQTT contract asks to be published separately.
> +    float pipeline_ms = 0.0f;
> +    float model_run_ms = 0.0f;
> +    float postprocess_ms = 0.0f;
993c1073
< @@ -20,13 +23,27 @@ struct PayloadContext {
---
> @@ -20,13 +31,27 @@ struct PayloadContext {
